# Source data cache

The reproducible build downloads source files into this folder. The cache is excluded from the submission package. Derived tables used by the story are included in the `story` folder.

